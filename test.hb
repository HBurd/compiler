main: (x: u32, y: u32)
{
    if x < y
    {
        z: u32 = 17;
        while x > x
        {
            x = x + y;
        }
        z = z + 2 * x + 3 * y;
        return z;
    }

    return 1;
}
